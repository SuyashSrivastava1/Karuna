import React from "react";
import ReactDOM from "react-dom/client";
import { NursePage } from "./app/components/NursePage";

ReactDOM.createRoot(document.getElementById("root")!).render(
    <React.StrictMode>
        <NursePage />
    </React.StrictMode>
);
