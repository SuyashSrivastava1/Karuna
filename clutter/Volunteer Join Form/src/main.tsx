import React from "react";
import ReactDOM from "react-dom/client";
import { VolunteerJoinPage } from "./app/components/VolunteerJoinPage";

ReactDOM.createRoot(document.getElementById("root")!).render(
    <React.StrictMode>
        <VolunteerJoinPage />
    </React.StrictMode>
);
